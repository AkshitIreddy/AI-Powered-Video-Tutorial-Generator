{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Core window/event access for the Alystria Studio application shell. Filesystem, shell, process and network plugins are intentionally absent.",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default"
  ]
}
