# September 16 product follow-up

Completed on local `main`. No push or release. Owner `see me` remains
untracked and untouched. The September 8 media qualification remains preserved.

## Delivered

- Removed wizard topic suggestions and the topic-specific placeholder. Home
  uses a neutral workflow explanation instead of a binary-search specimen.
  Typed topics no longer select canonical fixtures automatically.
- Removed monetary budget inputs, approval/cap gates, and known-price gates
  across frontend, Rust requests, contracts, jobs, and providers. Usage/cost
  receipts, privacy, credentials, consent, provider quotas, and bounded retries
  remain. RAM/VRAM resource limits remain.
- Added an explicit creation step for no presenter or up to four presenters,
  with search, style filters, and optional voice overrides. Plan → Presenters
  edits the cast and assigns speakers to scenes. Approval freezes the exact
  reviewed cast and customization. New tutorials default to no presenter.
- Added 12 original fictional presenter portraits: six anime settings and six
  additional illustration styles, with bundled provenance. The WebP assets
  total approximately 1.05 MB. Portrait inclusion does not qualify every
  animation model on every illustration style.
- Bound presenters and voices per scene in the pipeline/compositor, with
  explicit assignments or round-robin defaults, and at least one scene per
  selected presenter in mandatory cast mode. Legacy single-presenter projects
  remain supported. Multiple presenters take turns, not simultaneous faces.
- Onboarding now queues selected verified model packages after license
  acceptance, with native progress, retry/resume/reuse. No optional packs are
  selected by default. Unsupported candidates retain a clear unavailable
  installer state. Waiting requests can be cleared; already started downloads
  continue independently. Paper/plum styling matches the workbench.
- Reduced eager model-card rendering from 33 to 6 initially, staged lower
  provider sections, consolidated initial reads, and avoided re-rendering the
  setup page for timestamp-only download polling updates.
- Fixed cold worker readiness by deferring Windows voice discovery until
  synthesis. Fixed first-edit autosave initialization and durable persistence
  of cast/scene assignments. All six Plan steps now fit one desktop row.

## Focused commits

- `a3297c8` — bundled presenters and provenance.
- `4c90a96` — navigation rendering and idle provider updates.
- `4bb942c` — optional onboarding downloads and styling.
- `cb2c9fc` — open briefs, cast selection/routing, budget removal, contracts.
- `df2a220` — native request deserialization without a budget field.
- `f81bf90` — lazy Windows voice discovery and bootstrap cleanup.
- `9c5c172` — first-edit/cast persistence and six-step Plan navigation.

## Verification

- Desktop baseline: 263/263 tests in 36 files; typecheck, lint, production build.
  The new durable-cast regression failed before the correction. After the final
  save fixes, App plus save-lifecycle tests passed 33/33, including all eight
  save-lifecycle cases. Final desktop/native build passed; no claim of a new
  full 264-case green run.
- Contracts: 67/67; generated bindings current. Themes: 26/26; starter checks 4/4.
- Budget/provider pipeline: 311 passed, plus 18 jobs/background cases.
- Presenter/coordinator/runtime: 147 passed; relevant Ruff and mypy passed.
- Rust: 74/74, including a real loopback HTTP download and hash verification,
  optional setup defaults, and no-budget request deserialization.
- Startup: 26/26 targeted adapter/background tests. Generation enqueue remains
  fast even when Windows voice capability discovery would hang.
- Browser acceptance/app: 23 passed, 3 deliberate viewport skips, no failures.
  Desktop and 860px cast layouts were inspected. Final native screenshots also
  confirm readable presenter cards and one-row Plan navigation.
- Actual renderer fixture: two portraits in two scenes with two synthetic PCM
  tracks, 20 frames, 2.008 seconds, zero blank frames, 0.008 seconds A/V drift.
  This verifies routing/composition, not animation or audible voice quality.
- Onboarding queue tests exercise real bridge requests and license decisions;
  native clean setup verifies installer availability states. Huge model packs
  were not downloaded during this run; the native downloader uses a tiny real
  HTTP fixture for its transfer/hash acceptance.

## Final Windows package and native evidence

Launch: `E:\temp\AI Video Tutorial Generator Test Sandbox\App\AI Video Tutorial Generator.exe`

Package created at **2026-09-16 09:21:33 UTC**:
- Desktop SHA-256: `90afb042208e2be0fc41ca93dab0dcf3ab7d58b64053a00561eacbc1e79582a0`
- Worker SHA-256: `538cc7aa3041ad10c2b102dbefe6ccf66e0f15c2e55cda6b3aabf1f6f94c58da`
- All 774 runtime components matched hash and size, plus executable and runtime
  manifest hashes, at 09:22:17 UTC.

Evidence root: `E:\temp\AI Video Tutorial Generator Test Sandbox\Evidence`.

- `package-verification-20260916-first-edit.json`: final package hashes.
- `native-recovery/report.json`: passed at **09:22:55 UTC**. Visible native
  creation selects Daniel and Astrid, edits the first scene to Astrid, closes,
  restarts, reopens the durable project, verifies the cast and speaker, and
  cancels the blocked job. BLOCKED survives restart; CANCELLED persists.
- `recovery-isolation-20260916T092149996488Z/isolation-report.json`: exact owner
  directory restored, successful harness exit, no remaining processes.
- `native-clean-first-launch/report.json`: passed at **09:24:25 UTC** on the
  final package, with onboarding/replay/relaunch, zero seeded projects/jobs,
  normal close, worker exit, and owner-state restoration. Fresh WebView profile
  bootstrap was uninstrumented; subsequent launches used native WebView CDP.
- `multi-presenter-routing-20260916/integrated/integrated-evidence.json`:
  compositor/installed-renderer fixture proof and inspected scene images.
- Browser gallery images: `E:\temp\avt-sep16-acceptance\presenter-layout-final-stepper`.
- Portrait contact sheet: `E:\temp\alystria-presenters-20260916\presenter-library-contact-sheet.png`.

Native integration caught issues that source-only checks missed: a stale Rust
budget DTO, eager Windows voice discovery exceeding the startup deadline,
missing cast fields in durable saves, and version-zero initialization skipping
first-edit autosave. These are fixed and rebuilt. A separate test-selector
issue inspected an aria-hidden Jobs drawer as visible; the harness now checks
its DOM state correctly. Failed runs and their isolated profiles are preserved.
One early cleanup needed a manual contained-path restore after a WebView lock;
subsequent harness cleanup retries renames and owns failed bootstrap children.

## Navigation measurements and limits

Same native harness, same portable owner profile; click through two animation
frames, then separately wait for progressive sections and pending work. The
first visit in each process is called cold; this is not a cleared OS disk cache.
Timings are observations from this machine, not a broad benchmark.

| Native Models & Providers | Previous build | Final run 1 | Final run 2 |
| --- | ---: | ---: | ---: |
| First visit paint | 205.9 ms | 243.7 ms | 222.5 ms |
| First visit settled | 245.1 ms | 352.1 ms | 337.3 ms |
| First visit long-task time | 123 ms | 70 ms | 71 ms |
| Return visit paint | 144.3 ms | 107.1 ms | 108.1 ms |
| Return visit settled | 184.0 ms | 150.2 ms | 154.5 ms |
| Return visit long-task time | 54 ms | 0 ms | 0 ms |

Return visits improved about 25%; first-visit blocking work decreased, but the
progressive first-load sequence takes longer to settle. Do not describe all
navigation as faster. Other routes measured 53–138 ms on return visits; Library
varied and had one 50 ms long task. Further cold-provider-page tuning remains a
possible follow-up. Browser measurements use a different clock and are not
compared directly with these native numbers.

Reports under the evidence root:
- Baseline: `native-navigation-performance-20260916082055699/report.json`.
- Final: `native-navigation-performance-20260916092322882/report.json`.
- Repeat: `native-navigation-performance-20260916092452440/report.json`.

No app-provider generation, paid TTS, or local AI GPU work occurred. The new
portraits used the built-in image generation tool. No Deepgram, Inworld, or
Cartesia testing occurred. Removing app dollar caps does not alter provider
account pricing. Installer/signing/release and previously documented advanced
editor work remain separate tasks; this update does not claim those complete.
