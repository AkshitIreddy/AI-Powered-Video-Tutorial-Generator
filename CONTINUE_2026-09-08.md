# September 8 continuation checkpoint

The owner resumed the September 7 pause. **Work is active, not complete.**

## Latest checkpoint — September 8, final package running

- Desktop a4e4e8a and worker 69f8c59 are now packaged. At 11:45:26 UTC,
  independent verification matched all 762 components. Desktop SHA
  `b0030358655844836386ff396783b15f1a5fe7c78bc6955c8a1c28702df477f6`;
  worker SHA `67082407465d0a23136b4531a05be8682fa16c5a35581646a417d38307220432`.
- Full native cached-master acceptance is RUNNING, evidence class
  `final-editor-69f8c59`. Log in the external evidence parent:
  `native-final-editor-69f8c59-20260908.log`. Monitor folder:
  `native-editor-memory-observer-final-69f8c59-20260908`.
  Do not refresh the package or relaunch another native harness until normal
  closure is verified. The monitor requests cooperative stop if memory exceeds
  its limits; it does not force-kill or delete work.
- Native media-bin checks and root inspection of
  `Evidence/native-ui-acceptance/07a-usable-project-media.png` passed: Elena has
  her actual thumbnail and `image · Still image` label, with 38 unlinked
  references preserved behind the unchecked toggle.
- Actual current OTIO export parsed/round-tripped with official 0.18.1:
  `timeline-01a080d7-ebd3-72a3-b705-5dd0aa167afc.otio`, SHA
  `8034d7bcb17272d4a4cef4a28fdaa6b726755225e591ee143d2df0026bc6f696`.
  Report: `native-otio-parser-final-a4e4e8a-20260908/report.json` under the
  external evidence parent. 55 clips, five CAS references, 180 seconds.
- Final complete desktop suite PASSED: 255 tests in 35 files, 209.69 seconds.
  Log: `desktop-final-a4e4e8a-tests-20260908.log`.
- Next: await native normal close, inspect final audio and all 18 decoded
  image hashes against the reviewed c35 candidate, repeat clean/recovery on
  this exact package, and reconcile final docs/Git. No new AI calls or lease.

## Historical checkpoint — September 8, 11:36 UTC

- Full native run on desktop 88f8c5f / worker c35e657 PASSED, including real
  JSON/OTIO files, CAS reload, waveform, title at Y=-180, full editor export,
  Review playback, archive, and normal close. Finished 11:24:02 UTC, desktop
  exit 0, no descendants, no fallback/problems. Peak FFmpeg RSS 1.157 GiB;
  observer elapsed 974.391 seconds. CPU VP9 export is slow on this workload.
- Job `6dbf5fcc-4ef2-4d6f-842c-82f6afe5e5e8` produced
  `exports/editor/editor-a994c82c6755438ca89ac8787dff18c0.webm` in the Karatsuba
  project. SHA `e52292e62877734c3db4811c2a7cf3f8f23d184a906e90b2a34587a18484dfdb`.
  Full decode: 5,400 video frames, exactly 8,640,000 mono samples at 48 kHz,
  180 seconds of audio, zero clipping, -16.24 LUFS. HOWEVER default 64 kbps
  Opus raises reconstructed peak to -0.32 dBTP, short of the -1.5 dBTP target.
  Visual review of all 18 decoded checkpoints passed for teaching legibility,
  caption layout and title separation. This is not the final audio delivery.
  Evidence: `E:\temp\avt-final-media-inspection-20260907\candidate-editor-c35e657-20260908`.
- Real audio-only comparison: 96/128 kbps peaks -1.31/-1.33 dBTP; 192 kbps
  preserves -16.24 LUFS with -1.79 dBTP. Commit 69f8c59 uses 192 kbps delivery
  audio; editor engine version v6 invalidates earlier jobs. 22 pipeline tests
  pass with Ruff. No gain or narration content was changed.
- c993c18 improves the media bin: 14 usable media assets appear before 38
  unlinked starter references, which remain recoverable via a toggle. Full
  desktop suite 254 passed, typecheck/lint/build passed. Native toggle/recovery
  of all references passed with normal shutdown at 11:33:04 UTC.
- Native close-up then exposed still portraits classified as video. a4e4e8a
  corrects classification from CAS media type and adds image thumbnails. The
  focused 21-test suite, typecheck and lint pass. Desktop rebuild is running:
  `desktop-portrait-type-build-20260908.log` in the external evidence parent.
- Worker 69f8c59 is already built and installed, SHA
  `67082407465d0a23136b4531a05be8682fa16c5a35581646a417d38307220432`.
  Installed desktop is still c993c18 / b08cfdd67c188d8a7212135f134ae93a81d501e502b6c2e66fc6eb396d26f34a.
  Package verified all 762 components at 11:32:21 UTC. Clean first launch on
  that package passed, ownerStateRestored=true. No app is currently running.
- NEXT: package the a4e4e8a desktop with the same 69f8c59 worker, verify hashes,
  rerun the native media-bin close-up (assert portrait thumbnail/type), final
  clean/recovery checks, and full cached-master native export at 192 kbps.
  The final video's decoded PNG hashes can be compared to all 18 already
  inspected candidate PNGs; reread any changed pixels. Inspect final audio.
- Benchmark-only row-mt and cpu-used alternatives were tested in
  `vp9-profile-comparison-20260908`; no encoder-speed source change was made.
- Current E: free space was 336.8 GiB. Current deletion staging folder is
  `E:\uesless\AI Video Tutorial Generator Superseded Portable Payloads 2026-09-08`.
  No direct deletion, provider generation, GPU lease, push or release occurred.
  Owner `see me` remains the only untracked file.

## Historical checkpoint — September 8, decoded editor rejection and audio correction

- Full native run on desktop `88f8c5f` / worker `ac3594c` completed, including
  JSON/OTIO files, editor export, Review playback, archive, and normal shutdown.
  However **the edited video is REJECTED**, not accepted: job
  `8d1d4d2c-af80-4045-b1be-a73df26f36c6` produced 180 seconds of video but only
  56.3547 seconds of overlapping audio, with clipping. Opening test title also
  collided with burned captions. Harness success was insufficient evidence.
- Rejected video SHA: `8e12e157832a1204da4460306b4ced0da2edf3e0a988cde38068aea7cdeda25f`.
  Inspection folder was prematurely named `accepted-editor-media-ac3594c-20260908`
  under `E:\temp\avt-final-media-inspection-20260907`; treat it as REJECTED.
- `ce0a6c0` fixes audio timestamps after adelay, uses sample-accurate delays at
  48 kHz, avoids unnecessary atempo=1, and preserves intentional timeline gaps.
  Four real-FFmpeg regressions failed before and pass after (source video and
  narration, 44.1/48 kHz). Combined editor/service suite: 19 passed; Ruff/mypy pass.
- Real cached lesson audio rerun is exactly 180 seconds, peak 0.798424, zero
  clipped samples, -16.24 LUFS / -1.96 dBTP, matching the approved master.
  Evidence: `E:\temp\avt-final-media-inspection-20260907\editor-audio-clock-proof-20260908`.
  This is audio-stage proof, not a replacement native video acceptance.
- Separate-pass memory correction is proven: final full native render peaked
  at 1.169 GiB. No GPU lease or provider generation was used. App/worker exited
  normally with no orphan descendants. Clean first launch completed at
  10:45:07 UTC with ownerStateRestored=true on the ac3594c package.
- NEXT: add decoded-delivery duration/clipping gate, correct title placement,
  rebuild/package worker (current installed worker still ac3594c), rerun full
  native cached-master editor flow, inspect actual decoded replacement video,
  retake final recovery checks and update overall documentation. Existing bad
  edited Review output means harness must no longer assume master is selected.
- Approved master `e849d162-64fe-4d03-acb7-8bd75872836a` remains valid; reuse it.
  Owner untracked `see me` untouched. No push/release authorized.

## Historical checkpoint — September 8, 10:35 UTC

- Source HEAD `ac3594c` follows `ea34bdc`: editor audio is prepared as float PCM
  in its own FFmpeg pass, then encoded with the visual delivery. One timeout
  budget covers both passes. Editor job cache keys now have a separate engine
  version, so old editor jobs do not survive a rendering implementation change.
- The actual full native editor render exposed unbounded combined audio/video
  buffering: FFmpeg PID 42584 reached 19.27 GiB RSS and left 0.41 GiB available.
  Cooperative cancellation of job `37fad2bb-0a9f-4301-b1b9-aaf1b78a53b4` reached
  CANCELLED at 10:25:47.502 UTC. Desktop exited 0, no worker descendants remained,
  no shutdown problems, and free RAM recovered to 20.65 GiB. No GPU lease was
  held. The cancellation receipt is in the latest native-ui-acceptance evidence.
- Bounded original-graph 15-second reproduction hit the 6 GiB stop budget;
  video-only completed at 1.14 GiB. The source-corrected split audio/video run
  completed 15 seconds with audio at 1.146 GiB in 20.92 seconds. Filter-thread
  count alone did not fix the issue. Evidence under
  `E:\temp\avt-final-media-inspection-20260907\editor-memory-*-20260908`.
  `probe_editor_memory_20260908.py` preserves exact argv and resource reports.
- Current worker build is running from `ac3594c` into
  `E:\temp\AI Video Tutorial Generator\build\acceptance-editor-memory-v2-20260908`.
  Log: `sidecar-editor-memory-v2-build-20260908.log` in the evidence parent.
  The earlier memory worker (`96cd2469...`) lacks the engine-version correction;
  package the v2 build instead. Then verify all package hashes and repeat the
  full cached-master acceptance with independent memory monitoring.
- Last installed desktop is source `88f8c5f`, SHA
  `c1c9bfd74602b69972f8071dce145cc4a4d898b71d7f5f14a300c331983940fd`.
  Its installed worker is still `b9b09a9` / `f3cd1226...`; do not run another
  full editor render until the worker refresh. Desktop does not need rebuilding
  for the pipeline-only correction.
- Native editor entry, CAS image import, waveform, title edit, and reload now
  work. `4919ba0` fixed Rust rejecting the worker's `captions` field. `88f8c5f`
  fixed exports resolving unused starter-library hashes that were never imported
  into project CAS. Unused entries remain for relinking; used media is verified.
  Native error objects now retain readable messages. Full desktop suite: 253
  tests passed at `4919ba0`; later focused document/editor tests: 16 passed.
  Split-audio editor/export tests: 14 passed, followed by 3 service/cache tests;
  counts overlap. Ruff/mypy passed for the changed render path.
- Actual native JSON and OTIO documents were saved. Official OpenTimelineIO
  0.18.1 accepted the native OTIO, 55 clips / 5 file-backed scene references,
  exactly 180 seconds, source frame offsets matching cumulative promoted master
  boundaries. OTIO SHA `d5302a788d2b4056f56dd19e6b1b3391fa42ff9f121f7836254da8ceb60639a8`.
  Report: `native-otio-parser-88f8c5f-20260908\report.json` in the evidence parent.
  It is a timeline referencing local CAS files, not bundled media or a named-NLE
  compatibility claim. The native editor success screenshot was inspected.
- Retry harness now reuses its existing imported test image and title, and
  reports reuse, instead of accumulating duplicate test edits. Two prior test
  image imports are preserved as evidence; neither is placed on the timeline.

Next: package the v2 memory worker, repeat the cached-master native editor render
with a 6 GiB RSS / 3 GiB available-RAM stop observer, inspect full edited output,
then final clean-profile/recovery retakes and documentation reconciliation.
All active AI/model work is stopped; `gpu use.txt` is `no`.

## Earlier checkpoint — September 8, 10:06 UTC

This section supersedes the older running-master checkpoint below.

- Master `e849d162-64fe-4d03-acb7-8bd75872836a` **succeeded**: 180 seconds,
  1920×1080, 30 fps, H.264 NVENC/AAC, complete BT.709/sRGB transfer metadata.
  SHA-256 `904b5e414303b0f79b9e48230a728afdae907257282fd8109404d0305222f1a6`.
  Sixteen decoded checkpoints were individually inspected, including presenter,
  derivation, code, and recurrence. Audio measured -16.24 LUFS, -1.96 dBTP,
  zero clipped samples. This is sampled visual and measured audio evidence,
  not every-frame lip-sync verification or an auditory listening claim.
- Independent immutable receipt/CAS/renderer identity and 49-cue caption audit
  passed. Evidence is under
  `E:\temp\avt-final-media-inspection-20260907\accepted-master-media-20260908`
  and `promoted-master-independent-audit-20260908.json` in its parent.
  Master app shutdown was normal, desktop exit 0, no remaining owned processes.
- `0ed12c3` fixes OTIO typed ranges and Clip.2 media references. Official
  OpenTimelineIO 0.18.1 parses and round-trips actual source exports, including
  generic import without private metadata. Named NLE compatibility remains open.
- `b9b09a9` fixes editor composition color metadata; real decoded VP9 probe and
  12 editor Python tests passed. Its worker SHA is
  `f3cd12263c0276afe79d3dd2de521bec7bff9e8931418d6283803df40460453b`.
  Clean native launch and isolated recovery passed on that worker, with owner
  state restored and no orphan processes. Full native editor render is pending.
- The master-resume harness validates the immutable receipt, video bytes,
  generation, codec, and renderer identity before reuse. Two resume attempts
  exposed a hidden-drawer test issue (fixed) and a genuine Review selection bug.
  `396ea05` prioritizes current promoted master/edited output over refreshed
  generation jobs; four focused Review tests and typecheck passed.
- Native WebView browser downloads cancelled even a 28-byte JSON Blob after
  receipt of all bytes. Delayed revoke, shorter path, explicit Playwright
  acceptance, and an unreleased minimal Blob did not fix it. This isolated the
  failure to the download path; it does not establish a universal WebView bug.
  `b06ea6f` instead saves native editor documents through a narrow Rust command:
  project identity, ordinary exports directory, size/schema checks, unique short
  filenames, create-new semantics, sync, and read-back verification. Native
  CAS media gets file URLs for interchange. Frontend tests cover drive/UNC
  paths and error/retry; Rust test, typecheck, lint and desktop build pass.
- `86cd6ff` adds actual native JSON/OTIO file assertions to the full editor
  harness. Package refresh is active at this checkpoint; do not launch against
  a partially refreshed payload. Next verify with external
  `verify_package_b06ea6f.py`, then run `run_native_cached_acceptance_20260908.py`
  with `h264-hardware`, a unique evidence class, and the exact master ID above.
  This runner acquires **no GPU lease** and generates **no AI content**.
- Obsolete old Rust cache (10,667,826,966 bytes) was reversibly moved to
  `E:\uesless\AI Video Tutorial Generator Obsolete Rust Cache 2026-09-08`.
  Its adjacent MOVE-MANIFEST records 6,148 files, 664 directories, no reparse
  points. Space is reclaimed only when the owner deletes it. Current build,
  model, project, evidence, and other-project folders remain intact.

Remaining: finish native editor/document/Review/archive flow; parse the actual
native OTIO with official tooling; inspect edited output; retake final package
clean/recovery as appropriate; reconcile final docs and checklist honestly.
No push/release. Owner `see me` remains untracked and untouched.
Use [the September 7 handoff](CONTINUE_2026-09-07.md) for the preserved project,
approved generation, audio/presenter/caption hashes, rollback profiles, and old
interrupted render. Do not push or release. Keep commands and app tests hidden.

## Committed corrections

- `056af64`: durable cancellation wins when an active handler raises.
- `81b7d38`, `8d17455`: real subprocess cancellation regression; Windows Job
  Object owns renderer descendants. Killing only the parent was reproduced as
  a six-second inherited-pipe hang. The fixed live parent/child tests pass.
- `fea5b53`: renderer build identity invalidates native master job reuse and is
  bound in v2 immutable receipts; old receipts remain readable.
- `7c26f73`: native scene/master cancellation scopes and cancellable editor
  FFmpeg; cancellation checks before promotion.
- `408ced5`: cooperative acceptance stop coordinator. Write a JSON object with
  `schemaVersion: 1` and a reason to
  `E:\temp\AI Video Tutorial Generator Test Sandbox\Evidence\native-stop-request.json`.
  The active harness observes it, captures exact process identities, cancels
  its active export, posts WM_CLOSE, checks descendants, and writes receipts.
  Cancellation IPC is bounded. A hard process kill cannot run this cleanup.
- `1ce62bc`: compact-code annotations and computed output are 26-32px.
- `e739685`: portrait PIP teaching text no longer collides with the presenter;
  vertically stacked split/full placements retain their original width.
- `12b9db8`: Google API authentication headers are redacted.
- `dc8ed7c`: optional bounded Gemini 3.7 Flash image-review route. The runtime
  factory supports vision without requiring a Gemini writing route. Catalog
  identity is now `2026.09.08.1`.
- `48b533d`: bundled teaching assets preserve `element` identity through native
  customization and the editor. Recognized legacy included elements stay out
  of background selectors; intentionally uploaded backgrounds remain eligible.
- `d19eb01`: managed SDXL returns a validated runtime revision and install
  fingerprint. Restart rechecks run in the background; transient Verifying is
  memory-only, preserving the durable Ready receipt if the app closes mid-check.
- `30de70f`: dedicated local-image setup exposes SDXL installation and explicitly
  download-only FLUX/Z choices. Profile staging requires Ready, explicit
  activationBlocked=false, and complete identity fields from native status.

## Evidence

Python renderer/editor selection:49 pass. Native/editor selection:40 pass.
Provider/vision/runtime selection:70 pass. These overlap; do not sum them.
Frontend provider/catalog:28 pass. Scenes:54 pass, typecheck and scenes/renderer
builds pass. Python Ruff/mypy and provider catalog validation pass. Harness
coordinator:4 tests pass; these are scoped source results, not final package QA.
Root reran the entire desktop Vitest suite with packaged Node24:34 files,
244 tests pass in147.56s. This includes the combined element/local-image UI
changes and the positive install-receipt routing case; later negative activation
cases require their own focused result.
The two negative cases subsequently passed with the positive case:3/3 focused
receipt UI tests. Native model-download tests:8/8 pass, including the durable
Ready restart regression and missing-file failure/identity clearing.

Install verification boundary: exact pinned manifest, archive and model-file
hashes plus extracted Python/main.py entrypoint existence. This is not a hash
audit of every extracted runtime dependency and not a new GPU benchmark.
The manually installed `E:\temp\Alystria Local Image Lab` remains available;
it does not contain the managed portable archive. Do not claim it passed the
new managed-install restart validator or duplicate weights merely for QA.

Element adapter tests:17 pass; bundled assets:4 pass on Node24; native
customization:13 pass. Focused headless library E2E passes. Root inspected
`E:\temp\avt-audit-2026-09-05\library-proof\teaching-element-excluded-from-backgrounds.png`
and `saved-editor-included-assets-media-bin.png`: the underline is excluded
from canvas backgrounds and remains a ready, placeable editor image.
Root also inspected the Gemini filtered catalogue screenshot under
`E:\temp\avt-final-media-inspection-20260907\gemini-image-review-catalog-20260908`;
one matching review model, no page errors, no provider calls.

Actual saved-manifest landscape/portrait frames150/3000/3650:
`E:\temp\avt-final-media-inspection-20260907\compact-code-and-presenter-20260908-final-guarded`.
Root inspected corrected code/results and portrait opener. The older interrupted
native frame cache still contained4200/5300; root inspected readable recurrence
and complexity content and copied them to `current-native-capture`.

Gemini live attempt: one synthetic32x32 image, max256 output tokens, no private
content, no TTS, no retry. It returned TRANSIENT after about67 seconds. Receipt:
`E:\temp\avt-final-media-inspection-20260907\gemini-vision-one-call-20260908.json`.
Do not call that a successful hosted semantic review. Details are in
[the Gemini audit](docs/research/gemini-visual-review-2026-09-08.md).

New read-only promoted-master verifier:
`E:\temp\avt-final-media-inspection-20260907\audit_promoted_master_20260908.py`.
It requires v2 provenance, independently hashes installed renderer files and
compares their identity to the receipt, then checks exact49-cue caption hashes
and delivery bytes. Syntax checked; not yet run on a new promoted master.

## Current continuation checkpoint

Work remains active. The setup readability correction is committed as `3be9607`
and passed fresh desktop/narrow browser checks. Later integrated tests exposed
and corrected real runtime defects:

- `7fc402b`: RPC master submission now supplies the declared renderer identity;
  execution independently verifies installed bytes before rendering.
- `4829161`: export identity includes Node, Chromium, FFmpeg, and shared libraries.
- `694362f`: pins the dated LGPL shared FFmpeg 8.1.2 build, including all ten
  binaries/DLLs and its license. The old FFmpeg 9 build required NVENC API13.1,
  but this driver supports13.0. Both H264 and HEVC one-frame probes passed with
  the new build. No driver changes or restricted-provider TTS were used.
- `ce2add4`: permanent desktop worker closure prevents late WebView requests
  restarting workers after shutdown. Seven native supervisor tests passed.
- `43d1eeb`: cached cancelled exports say cancelled, and successful ones say ready.
- `ea5c935`: writes BOM-free package metadata under Windows PowerShell5.1 and7,
  accepts older BOM-prefixed metadata, and rejects malformed portable metadata
  instead of silently selecting a different profile. Five native state tests passed.
- `e44d700`, `f358cb9`, `97dd51e`: exact cancelled-master UI retry and stronger
  process inventory/cleanup evidence. The final approach verifies incomplete rows
  from a fixed snapshot instead of chasing newer short-lived Chromium children.
  Sixteen harness tests passed. Earlier failed attempts remain preserved.

The missing portable feature in one desktop build was investigated, but it was
NOT the startup root cause. Both feature-enabled and historical executables
failed with BOM-prefixed metadata. Removing the BOM restored the actual worker
handshake. Temporary startup tracing was removed before `ea5c935`.

## Current package and accepted cancellation

Desktop source: `ea5c9358aeb72321fe9a0412a0ea5f60da71f983`.
Worker source: `694362f`; subsequent changes have not altered pipeline source.
Acceptance harness: `97dd51e`.

Root independently verified all762 runtime components and BOM-free metadata:
`E:\temp\AI Video Tutorial Generator\build\acceptance-ea5c935\root-package-verification.json`.

- Desktop SHA256: `01976a5e16fd63d2329a0a07b0bd7224cbea70e3fa6eb0e18b28e5d0496ba518`
- Worker SHA256: `aca5806cc6a76298bd1265f87fe76c14faa5f70dead5e38afdfbedabd656c85c`
- Runtime manifest SHA256: `98f42c5df52b9f1a685e8b64e7a24e9648256b54d8db114e8324680b39058665`
- Test-area manifest SHA256: `745533dd474ea751b95099472f3673372e6fe10f2d29ac093e8b1aaea58fd833`

Actual cancelled HEVC job `f02fcaa6-fda4-4df4-8aca-825c3b3605cc`, attempt4,
reached CANCELLED at08:08:21.418UTC with no result. The final stop captured18
owned process identities, closed desktop6520 with exit0, found no remaining
processes, no fallback, and no problems. Root independently queried SQLite
and scanned the sandbox executable paths. GPU ownership returned to `no`.
Accepted evidence:
`E:\temp\avt-final-media-inspection-20260907\native-cancellation-accepted-ea5c935`.

Earlier cancellation attempts exposed a genuine worker restart race and later
CIM inventory races; they are not retroactively accepted. Exact leaked workers
from the first attempt were identity-checked and stopped. Later attempts left
no survivors and needed no forced cleanup, but their snapshot gates failed.

## Earlier full-length run (failed; superseded below)

The earlier H264 master/editor harness started at about08:09UTC. Native master job:
`1a84146b-c7c0-43fe-b378-ce6d8fea0cbf`. It uses the existing successful generation,
Aria narration, Elena presenter, and49-cue captions; no AI regeneration is needed.

Renderer attempt:
`E:\temp\AI Video Tutorial Generator Test Sandbox\Projects\explain-karatsuba-multiplication-using-12-34-derive--mtoekhmp\staging\renderer\attempt-25e3298797828cd6`.
Progress is in `output/render-progress.jsonl`. Its frame cache is
`output/.render-cache/321c24563a6a9c3417700d503bc1cde86ec7c4ac14a8c7df98fb92821ea38eae/frames`.
Root inspected actual frame150: visible presenter and no teaching-text collision.
This attempt subsequently failed the delivery color metadata gate. It was not
promoted and must not be treated as a completed master.

The guard owns the shared GPU marker during this run. On a user pause, use the
cooperative stop-request file described above; do not kill the harness first.
The root external helper is
`E:\temp\avt-final-media-inspection-20260907\run_native_acceptance_guarded_20260908.py`.
Its H264 run continues through promoted-media playback, full editor export,
archive, and normal shutdown. Preserve failure receipts if any stage fails.

After completion, run the independent promoted-master/caption verifier and
media inspection helpers in that same external evidence directory. Inspect
actual final teaching/code frames and decoded audio measurements. Then repeat
clean-profile and recovery checks against this exact final package. The earlier
successful BOM diagnostic onboarding run used a traced executable with stale
package metadata; its scope.json explicitly excludes it from final qualification.

`native-clean-first-launch.mjs` isolates/restores App Data itself. The recovery
harness does not: wrap it with explicit same-volume owner-profile preservation,
then restore only after owned processes exit. Do not overwrite the accepted
project/profile or other agents' work. Deferred quarantine stays deferred until
final media proof and process shutdown. Owner `see me` remains untouched.

## Package checkpoint: 09:10 UTC, September 8

The earlier full render completed encoding but failed strict QA: its H264 stream
reported `bt709/unknown/unknown` instead of Rec.709/sRGB metadata. Normal app
shutdown passed without surviving owned processes; GPU ownership returned to
`no`. The failed attempt and its evidence remain preserved.

`4aa8ad5` corrects delivery encoding by explicitly converting the matrix/range
and setting frame color properties before encoding. Output flags alone were
reproduced as insufficient. Actual source-planned three-frame H264, HEVC8,
HEVC10, and VP9 probes all report the intended metadata. Renderer tests passed
(25 passed, 3 opt-in media tests skipped). This is not full-master qualification.

`a69613c` makes failed Jobs readable with a concise summary and expandable,
bounded technical details. Desktop and narrow browser checks passed; root
inspected collapsed and expanded screenshots. `13cc34f` separates onboarding
model selections from verified installations and fixes download estimates.
Component/state checks and typecheck passed; current native onboarding passed.
Root inspected `02-model-toolkit-availability.png`: selected models explicitly
say installation is unverified and the download estimate remains visible.

Current desktop/renderer package source: `13cc34f04b648bbf36da50608fad19b747c7d3d4`.
Pipeline source remains `694362f` (no later pipeline source changes).
All 762 runtime components were independently verified at 09:02:39 UTC:
`E:\temp\AI Video Tutorial Generator\build\acceptance-13cc34f\root-package-verification.json`.

- Desktop SHA256: `fdd1ce9a2be9c8af0f97c8d89a4c9fa921840078cfe1403978096302b05920a1`
- Worker SHA256: `aca5806cc6a76298bd1265f87fe76c14faa5f70dead5e38afdfbedabd656c85c`
- Runtime manifest SHA256: `33e7a896e2ffa8b6eeb9563fd3eef2c27c08863b6cc367722eb372174ad98052`
- Test-area manifest SHA256: `ce6956fce3722cc88173e6b936a9edb8c133983bf7417a5fc24fed3d650e97f2`

Current-package clean first launch completed at 09:03:17 UTC: no seeded jobs or
projects, onboarding/replay/relaunch passed, graceful shutdown and worker exit,
original App Data restored. Evidence is under the sandbox's
`Evidence\native-clean-first-launch`. Uninstrumented bootstrap proves native
readiness; instrumented later launches prove the UI flow.

An isolated recovery retake passed and restored owner state at 09:09:20 UTC.
The same blocked job survived restart and reached CANCELLED through the UI;
both app/worker shutdowns passed, and the independent remaining-process list
was empty. Root inspected both before/after native screenshots. Its wrapper is
`E:\temp\avt-final-media-inspection-20260907\run_recovery_isolated_20260908.py`.
The earlier wrapper attempt omitted the selected test route and failed before
creating a job; owner state was restored. The corrected wrapper copies only
`model-setup.json` into the isolated profile. Current recovery evidence:
`E:\temp\AI Video Tutorial Generator Test Sandbox\Evidence\recovery-isolation-20260908T090756363854Z`.
Its isolation report confirms the original owner directory identity was restored.

Still required: full native master/editor export with
the color fix; independent promoted-media/caption verification and rendered
frame review; final documentation and any safe deferred quarantine. No provider
generation was added during these fixes. The existing successful narration,
presenter, approved project, and captions should be reused.

## Active continuation: 09:29 UTC, September 8

Current-package cancellation also passed: HEVC job
`d1ad0f22-5484-432d-ac15-b1ab515d2cf0` reached CANCELLED at 09:11:43.263 UTC,
with null result. Normal close ended at 09:11:51.161 UTC: exit zero, no remaining
processes, no fallback, no problems. Root independently queried SQLite and
scanned sandbox executable paths. Evidence is preserved in
`E:\temp\avt-final-media-inspection-20260907\native-cancellation-accepted-13cc34f`.

The new H264 master job `e849d162-64fe-4d03-acb7-8bd75872836a` is running.
Renderer attempt is `attempt-378df7206c8e0b6d`; all 5,400 frames captured in
about 715 seconds, and presenter composition started at 09:26:55 UTC after
lossless base encoding. The renderer is unchanged by the newer editor fixes.
Its runtime identity is
`e984b5d4a479cb84be1a87747d2a583a097e4e0040f907dc34f975011a9b97b2`.

Two additional defects were independently reproduced and corrected during the
render:

- `0ed12c3`: official OpenTimelineIO 0.18.1 rejected missing typed time schemas
  and mismatched Clip.2 media references. Corrected exports pass the official
  parser and generic round-trip after stripping private metadata. Gap, source
  trim, duration and URI remain exact. 23 focused tests, typecheck, ESLint, and
  the feature-enabled Windows desktop build pass. Named-NLE parity is unproven.
- `b9b09a9`: the editor composite discarded color primaries/transfer even when
  its input had correct tags. Explicit final conversion and frame/stream tags
  preserve all four fields in a real three-frame probe. All 12 editor-export
  tests and Ruff pass. The new sidecar build completed with SHA256
  `f3cd12263c0276afe79d3dd2de521bec7bff9e8931418d6283803df40460453b` at
  `E:\temp\AI Video Tutorial Generator\build\acceptance-editor-color-20260908\sidecar-output\alystria-pipeline.exe`.

Do not replace the live package yet. `stop_after_master_20260908.py` in the
external evidence directory watches only the exact H264 job above and requests
cooperative shutdown after successful promotion. This avoids rendering the
full editor output with the known old encoder. Wait for master/stop/shutdown
receipts and empty process scan; preserve them; then refresh the portable copy
using the new desktop build and new worker. Reuse the verified master through
the native UI and complete the corrected full editor export.

The prepared external `native_editor_interchange_20260908.mjs` checks an actual
OTIO download from the native editor and normal shutdown. It is syntax checked
but has not run. Use it after package refresh and after the full native harness
exits; it must not compete for the same app profile. Final package hashes,
current clean/recovery checks after the worker refresh, master/caption/media
audit, full editor output review, and deferred quarantine remain required.
